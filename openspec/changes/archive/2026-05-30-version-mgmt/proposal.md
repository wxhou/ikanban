# 版本管理

## 背景

100+ 版本需要管理。目前任务混在一起，没有迭代概念。

## 范围

- 数据库新增 `versions` 表，tasks 加可空 `version_id`
- FilterBar 新增版本下拉筛选
- TopNav 新增"版本管理" Tab（新建/结算/重命名/激活/删除）
- TaskModal 新建时自动关联当前筛选的版本
- Dashboard/Report 按版本过滤
- 筛选已结算版本 → 看板只读

## 不做什么

- 不做版本对比/图表
- 不强制任务必须关联版本
